/*You are required to complete this method */
int atoi(string str)
{
    //Your code here
    for(int i=0;i<str.length();i++)
    if(str[i]>=65&&str[i]<=123  )
       return -1;
    return stoi(str);
}
